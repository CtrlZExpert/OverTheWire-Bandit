# Level 06 — Spaces in filenames

### Challenge
Open a file whose name contains spaces.

### Solution
```bash
cat "spaces in this filename"
```

### What I Learned
Quoting paths handles spaces safely.

### Key Takeaway
I always quote user-provided paths to avoid errors.
